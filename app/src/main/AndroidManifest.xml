<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.redentor.redgames">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/redentor"
        android:label="@string/app_name"
        android:roundIcon="@drawable/redentor"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RatingActivity"></activity>
        <activity android:name=".ChooseTeamVotationActivity" />
        <activity
            android:name=".DetailsTeamActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".FullDescriptionGameActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".DescriptionGamesActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>